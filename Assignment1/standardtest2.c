#include <stdio.h>

int main() {

    int a, b, c, d, sink, source;
 
    if (a > 0){
      c = source; 
    } else { 

    }

   if (b > 0){
      sink = c;
      sink = 7; 
    } else { 
    }

    // Is secret printed?
    printf("%d\n", sink);
}
 
