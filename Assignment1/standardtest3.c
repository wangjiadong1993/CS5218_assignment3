#include <stdio.h>

int main() {

    int a, b = 0, c, d, sink, source = 1234;
 
    if (a > 0){
      c = source; 
     } else { 
      c = source + d; 
    }

    sink = c;
    
    // Is secret printed?
    printf("%d\n", sink);
}
 
